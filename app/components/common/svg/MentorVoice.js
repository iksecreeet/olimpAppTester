import React from 'react';

import Svg, { Path } from 'react-native-svg';

const MentorVoice = (viewbox,width, height) => {
	return (
		<Svg 
		viewBox={viewbox}
		width="width"
		height="height"
		>
		<Path
		/>
		</Svg>
	);
};

export { MentorVoice };
